---
title: "Claude Code 接入"
description: "基于官方安装方式和第三方 provider 接入能力，把 Claude Code 接到 Xcompute。"
---

这篇指南基于 Claude Code 官方文档整理，适合想在终端中直接使用 Xcompute 的开发者。内容分为三块：安装、接入、日常使用。

<Frame>
  ![Claude Code 界面示例](/images/image-3.png)
</Frame>

<caption>*Claude Code 在终端中的典型工作界面*</caption>

## 先理解 Claude Code 是什么

Claude Code 是 Anthropic 的编码 Agent。它能直接在终端里：

1. 理解整个仓库结构
2. 跨多个文件修改代码
3. 运行命令、分析报错、给出修复方案
4. 配合 `CLAUDE.md`、MCP、规则文件做持续化工作流

### 系统要求

| 项目 | 最低版本 | 推荐版本 |
| --- | --- | --- |
| Node.js | v22.0.0 | v22.x LTS |
| npm | v10+ | 最新稳定版 |
| 操作系统 | macOS 12+ / Ubuntu 20.04+ / WSL2 | 最新稳定版 |
| 内存 | 4 GB | 8 GB+ |
| 磁盘空间 | 500 MB | 1 GB+ |

<Tip>
  Claude Code 不支持原生 Windows（cmd / PowerShell），必须通过 WSL2 使用。
</Tip>

## 这篇文章会带你完成什么

你做完这篇后，应该能完成这几件事：

1. 在本地安装 Claude Code
2. 用 Xcompute 的 `Base URL` 和 `API Key` 接入
3. 在你的项目目录里启动 Claude Code
4. 执行第一次问答和第一次代码修改任务

## 安装方式

如果你还没有准备 Node 环境，先看 [环境准备：NVM、Node.js 22 和 npm](/node-setup)。

官方更推荐原生安装脚本，但如果你想和 `Codex`、`OpenCode` 统一走 Node 环境，也可以直接用 npm。

<Tabs>
  <Tab title="npm 安装（推荐本教程用这个）">
    ```bash
    npm install -g @anthropic-ai/claude-code
    ```
  </Tab>

  <Tab title="官方原生安装">
    macOS / Linux / WSL:

    ```bash
    curl -fsSL https://claude.ai/install.sh | bash
    ```

    Windows PowerShell:

    ```powershell
    irm https://claude.ai/install.ps1 | iex
    ```
  </Tab>
</Tabs>

安装后验证：

```bash
claude --version
```

如果能看到版本号，说明工具本身已经安装成功。

### 各平台安装说明

**macOS**

```bash
# 如果使用 Homebrew 管理的 Node.js
brew install node@22
npm install -g @anthropic-ai/claude-code
```

**Ubuntu / Debian**

```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs
npm install -g @anthropic-ai/claude-code
```

**Windows (WSL2)**

```powershell
# 1. 先在 PowerShell 中安装 WSL2
wsl --install

# 2. 重启电脑后进入 WSL Ubuntu
wsl

# 3. 在 WSL 中安装 Node.js 和 Claude Code
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs
npm install -g @anthropic-ai/claude-code
```

## 接入 Xcompute

Claude Code 支持第三方 provider，这是接入 Xcompute 的关键。Xcompute 的 API 客户端会与 Anthropic Messages 协议进行转换，所以你只需要配置 Base URL 和 Key 即可。

### 了解渠道信息

| 配置项 | 值 |
| --- | --- |
| Base URL | `https://xcompute.us` |
| 加速入口 | `https://intl.dualseason.com` |
| API Key 格式 | `sk-xxxxxxxxxxxxxxxx` |
| 协议 | Anthropic Messages |

渠道分组与折扣：

| 渠道名称 | 折扣 | 说明 |
| --- | --- | --- |
| GPT 福利渠道 | 0.9 折 | 超低价格，适合日常开发 |
| Kiro 渠道 | 1.8 折 | 平衡价格与性能 |
| 官 Key 渠道 | 4.9 折 | 接近官方价格，稳定性最佳 |

### 三种配置方式

Mintlify 不支持嵌套 Tabs，这里列出三种方式，选择一种即可。

**方式一：环境变量（推荐）** — 优先级最高，最简单直接

```bash
# 永久写入配置（bash）
echo 'export ANTHROPIC_BASE_URL=https://xcompute.us' >> ~/.bashrc
echo 'export ANTHROPIC_AUTH_TOKEN=sk-你的API密钥' >> ~/.bashrc
source ~/.bashrc

# 或如果你用 zsh（macOS 默认）
echo 'export ANTHROPIC_BASE_URL=https://xcompute.us' >> ~/.zshrc
echo 'export ANTHROPIC_AUTH_TOKEN=sk-你的API密钥' >> ~/.zshrc
source ~/.zshrc
```

临时测试（当前终端有效）：

```bash
export ANTHROPIC_BASE_URL=https://xcompute.us
export ANTHROPIC_AUTH_TOKEN=sk-你的API密钥
claude
```

**方式二：claude config 命令**

```bash
claude config set anthropicBaseUrl https://xcompute.us
claude config set apiKey sk-你的API密钥
claude config list   # 查看当前配置
```

配置写入 `~/.claude/settings.json`，可手动编辑。

**方式三：手动编辑 settings.json**

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "https://xcompute.us",
    "ANTHROPIC_AUTH_TOKEN": "YOUR_XCOMPUTE_API_KEY",
    "ANTHROPIC_MODEL": "claude-sonnet-5"
  }
}
```

文件位置：
- macOS / Linux：`~/.claude/settings.json`
- Windows：`C:\Users\你的用户名\.claude\settings.json`

### 加速入口

如果 `https://xcompute.us` 延迟较高，可切换加速入口：

```bash
export ANTHROPIC_BASE_URL=https://intl.dualseason.com
```

两个入口使用相同 API Key，协议完全一致，均可正常使用。

| 对比项 | 标准入口 | 加速入口 |
| --- | --- | --- |
| URL | `https://xcompute.us` | `https://intl.dualseason.com` |
| 协议 | Anthropic Messages | Anthropic Messages |
| 适用场景 | 默认使用 | 网络较慢时切换 |
| API Key | 通用 | 通用 |

### 模型选择

| 模型 | 特点 | 适用场景 |
| --- | --- | --- |
| `claude-sonnet-5` | 速度与质量平衡 | 日常开发推荐 |
| `claude-opus-4-8` | 最强推理能力 | 复杂架构、疑难 bug |
| `claude-fable-5` | 推理增强 | 复杂推理任务

### 验证连接

```bash
claude "你好，请用一句话介绍你自己"
```

如果能正常返回内容，说明已接通 Xcompute。如果跳到官方登录流程，检查配置路径或重开终端。

连接检查清单：

-  `claude --version` 正常输出版本号
-  `echo $ANTHROPIC_BASE_URL` 输出 `https://xcompute.us`
-  `echo $ANTHROPIC_AUTH_TOKEN` 输出你的 API Key
-  `claude` 可以正常启动并对话

## 高级配置

### 多模型切换

```bash
# 启动时指定模型
claude --model claude-opus-4-8

# 运行时切换（在 Claude Code 界面中）
/model claude-sonnet-5
```

推荐创建别名（写入 `~/.zshrc` 或 `~/.bashrc`）：

```bash
alias cc-sonnet='claude --model claude-sonnet-5'
alias cc-opus='claude --model claude-opus-4-8'
alias cc-quick='claude --model claude-fable-5'
```

### 代理设置

```bash
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
export NO_PROXY=localhost,127.0.0.1
```

<Tip>
  Xcompute 中转站通常不需要代理。代理主要在使用官方 API 时使用。
</Tip>

### 超时配置

```bash
export CLAUDE_CODE_TIMEOUT=180000   # 请求超时（毫秒）
export CLAUDE_CODE_CONNECT_TIMEOUT=30  # 连接超时（秒）
```

### 自定义 System Prompt

在项目根目录创建 `CLAUDE.md`，Claude Code 会自动读取作为上下文参考：

```md
# 项目说明

这是一个 React + TypeScript 项目。

## 编码规范
- 使用函数式组件和 React Hooks
- 所有函数必须有 TypeScript 类型注解
- 组件文件使用 PascalCase 命名
- 工具函数使用 camelCase 命名
```

### 权限精细管理

```bash
claude config set permissions.allow "Read"
claude config set permissions.allow "Write:src/**"
claude config set permissions.deny "Bash:rm -rf *"
claude config set permissions.deny "Bash:sudo *"
```

对应 `settings.json` 配置：

```json
{
  "permissions": {
    "allow": ["Read", "Write:src/**", "Bash:npm *", "Bash:git *"],
    "deny": ["Bash:rm -rf *", "Bash:sudo *"]
  }
}
```

### 管道输入用法

`claude -p` 支持非交互模式，适合脚本集成：

```bash
# 分析代码
cat src/app.ts | claude -p "分析这段代码的潜在问题"

# 审查 Git diff
git diff HEAD~3 | claude -p "审查这些变更，指出潜在问题"

# 日志分析
cat error.log | claude -p "分析错误日志，找出根因"
```

## 第一次正式使用：一步一步来

### 1. 进入你的项目目录

```bash
cd your-project
```

### 2. 启动 Claude Code

```bash
claude
```

### 3. 先做一个简单问题

```text
请先告诉我这个仓库是做什么的
```

### 4. 再做一个低风险任务

```text
请帮我找出这个项目的首页文件在哪里
```

### 5. 最后再让它改代码

```text
请帮我在首页加一句欢迎文案
```

这样最不容易一下子把操作做复杂。

## 日常怎么用

### 1. 在项目目录中启动

```bash
cd your-project
claude
```

### 2. 让它解释代码

```text
帮我解释一下这个仓库的认证流程
```

### 3. 让它修改代码

```text
请在这个项目里增加一个登录失败重试提示
```

### 4. 常用命令

1. `/help`：查看帮助
2. `/model`：切换模型
3. `/clear`：清空当前界面
4. `/exit`：退出

## 常见问题

### API 连接失败

```
Error: Failed to connect to API endpoint / ECONNREFUSED / ETIMEDOUT
```

排查步骤：

```bash
# 1. 检查环境变量
echo $ANTHROPIC_BASE_URL   # 应输出 https://xcompute.us

# 2. 测试网络连通性
curl -v https://xcompute.us
# 返回 401 说明连接正常，问题在 API Key

# 3. 检查 DNS 解析
nslookup xcompute.us

# 4. 尝试切换加速入口
export ANTHROPIC_BASE_URL=https://intl.dualseason.com
```

### 认证错误

```
Error: Authentication failed (401) / Invalid API key
```

排查步骤：

```bash
# 1. 确认 API Key 格式
echo $ANTHROPIC_AUTH_TOKEN   # 应以 sk- 开头

# 2. 检查是否有多余空格
echo -n "$ANTHROPIC_AUTH_TOKEN" | wc -c

# 3. 用 curl 直接测试
curl https://xcompute.us/v1/messages \
  -H "x-api-key: $ANTHROPIC_AUTH_TOKEN" \
  -H "content-type: application/json" \
  -H "anthropic-version: 2023-06-01" \
  -d '{"model": "claude-sonnet-5", "max_tokens": 10, "messages": [{"role": "user", "content": "Hi"}]}'
```

<Warning>
  API Key 是从 Xcompute 平台获取的，不是 Anthropic 官方 Key，两者不通用。
</Warning>

### 模型不可用

```
Error: Model not found / Model not available
```

```bash
# 1. 确认模型名称完整
echo $ANTHROPIC_MODEL   # 应输出 claude-sonnet-5 等

# 2. 清除模型配置，使用默认
unset ANTHROPIC_MODEL
claude config remove model

# 3. 访问 Xcompute 控制台查看可用模型
```

常见错误：

| 错误写法 | 正确写法 |
| --- | --- |
| `claude-sonnet-4` | `claude-sonnet-5` |
| `claude-4-opus` | `claude-opus-4-8` |
| `claude-4` | `claude-sonnet-5` |

### 网络超时

```
Error: Request timed out / ETIMEDOUT after 30000ms
```

```bash
# 1. 切换到加速入口
export ANTHROPIC_BASE_URL=https://intl.dualseason.com

# 2. 增加超时
export CLAUDE_CODE_TIMEOUT=180000

# 3. 检查代理设置
echo $HTTP_PROXY
echo $HTTPS_PROXY

# 4. 清除代理（如果导致问题）
unset HTTP_PROXY
unset HTTPS_PROXY
```

### 与官方 API 的区别

| 对比项 | 官方 API | Xcompute |
| --- | --- | --- |
| Base URL | `https://api.anthropic.com` | `https://xcompute.us` |
| 认证方式 | `x-api-key` header | `ANTHROPIC_AUTH_TOKEN` |
| 价格 | 官方定价 | 折扣价（最低 0.9 折） |
| 计费方式 | 按 token 直接计费 | 通过 Xcompute 平台 |
| 稳定性 | 最高 | 取决于中转站负载 |

### 为什么建议先从简单问题开始？

因为非技术用户最容易在第一步就直接让 Agent 做复杂改动。先确认它能理解仓库、能正常响应，再开始真正修改更稳。

## 配置速查

| 配置项 | 环境变量 | 命令 | 示例值 |
| --- | --- | --- | --- |
| API 地址 | `ANTHROPIC_BASE_URL` | `claude config set anthropicBaseUrl` | `https://xcompute.us` |
| API 密钥 | `ANTHROPIC_AUTH_TOKEN` | `claude config set apiKey` | `sk-xxx` |
| 模型 | `ANTHROPIC_MODEL` | `claude config set model` | `claude-sonnet-5` |
| HTTP 代理 | `HTTP_PROXY` | — | `http://127.0.0.1:7890` |
| HTTPS 代理 | `HTTPS_PROXY` | — | `http://127.0.0.1:7890` |
| 超时 | `CLAUDE_CODE_TIMEOUT` | — | `180000`（毫秒） |

## 配图示例

<Frame>
  ![Claude Code 终端会话](/images/image-3.png)
</Frame>

<Frame>
  ![Claude Code 多步任务场景](/images/image-4.png)
</Frame>

<Info>
  建议不要把 Claude Code 和生产应用共用同一个密钥。按工具拆分 API Key，更方便限额管理和排查问题。
</Info>

## 官方参考

1. [Claude Code overview](https://docs.anthropic.com/en/docs/claude-code)
2. [Claude Code setup](https://docs.anthropic.com/en/docs/claude-code/setup)
3. [Claude Code best practices](https://docs.anthropic.com/en/docs/claude-code/best-practices)
4. [Xcompute API 文档站](https://api.xcompute.us)

## 一键配置脚本

保存为 `setup-claude-code.sh`，在 macOS / Linux 上运行：

```bash
#!/bin/bash
set -e

echo "配置 Claude Code + Xcompute..."

if ! command -v node &> /dev/null; then
    echo "Node.js 未安装，请先安装 Node.js 22+"
    exit 1
fi

NODE_VERSION=$(node --version | cut -d'v' -f2 | cut -d'.' -f1)
if [ "$NODE_VERSION" -lt 22 ]; then
    echo "Node.js 版本过低 ($(node --version))，需要 v22+"
    exit 1
fi

npm install -g @anthropic-ai/claude-code

read -p "请输入你的 Xcompute API Key: " API_KEY
echo "请选择 API 入口："
echo "  1) 标准入口 (https://xcompute.us)"
echo "  2) 加速入口 (https://intl.dualseason.com)"
read -p "请输入选项 (1/2): " ENTRY

if [ "$ENTRY" = "2" ]; then
    BASE_URL="https://intl.dualseason.com"
else
    BASE_URL="https://xcompute.us"
fi

SHELL_RC="$HOME/.zshrc"
[ -n "$BASH_VERSION" ] && SHELL_RC="$HOME/.bashrc"

echo "" >> "$SHELL_RC"
echo "# Xcompute Claude Code" >> "$SHELL_RC"
echo "export ANTHROPIC_BASE_URL=$BASE_URL" >> "$SHELL_RC"
echo "export ANTHROPIC_AUTH_TOKEN=$API_KEY" >> "$SHELL_RC"

echo "配置已写入 $SHELL_RC"
echo "运行 source $SHELL_RC 生效，然后执行 claude 开始使用"
```
